#include <bits/stdc++.h>
using namespace std;

int t, n, a, b, start, dest;
int record[200005];

int main() {
    cin>>t;
    while (t--) {
        cin>>n>>a>>b;
        memset(record, 0, n);
        for (int i = 0; i<n; i++ ) {
            cin>>start>>dest;
        }
        
}